CREATE TABLE LISTINGS (
    ID INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    TITLE VARCHAR(2147483647) DEFAULT 'bau' NOT NULL,
    SUBMIT_DATE DATE DEFAULT NULL,
    NO_ROOMS SMALLINT DEFAULT 0
);